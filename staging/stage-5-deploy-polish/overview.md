# Stage 5 — Deploy & Polish

## Goal

Ship it: automated build + deploy to GitHub Pages, and a general cleanup pass before calling v1 done.

## Features in this stage

- `feature-github-actions-deploy.md` — CI build + Pages deploy workflow.

## Definition of done

- [ ] Pushing to `main` automatically builds and deploys the latest version to GitHub Pages.
- [ ] `docs/master_plan.md` and `handoff.md` reflect the real, final v1 state.
